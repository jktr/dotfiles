# https://neomutt.org/feature/encrypt-to-self
set pgp_self_encrypt = yes

# https://neomutt.org/feature/forgotten-attachment
set abort_noattach = ask-yes
set attach_keyword = "(attach|attached|attachments?|anbei|[Aa]nhang|anhängen|angehängt|anhänge|hängt an)"

# https://neomutt.org/feature/new-mail
set new_mail_command="notify-send --app-name mutt 'new email' '%n new / %u unread in %b folders' &"

# https://neomutt.org/feature/progress
color progress brightwhite red
