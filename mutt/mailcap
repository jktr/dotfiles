### .mailcap
## see RFC1524 for details

text/html;              w3m -T text/html %s; copiousoutput

image;                  feh %s;  test=test -n "$DISPLAY"
image;                  mpv --vo=drm --pause %s

application/pdf;        evince %s; test=test -n "$DISPLAY"
application/pdf;        less %s; needsterminal

application;            libreoffice %s; test=test -n "$DISPLAY" # mostly MS Office attachments
